# Approved-models registry for qwen-run.sh --approved-only.
# One model id per line. Lines starting with # and blank lines are ignored.
# Approval is per exact id (quant-specific) - a new quant or checkpoint
# needs its own eval before it's added here.

# Qualified 2026-07-12: 6-task agentic eval, 5/6 sonnet-tier Rust tasks
# passed, served via llama.cpp.
unsloth/Qwen3-Coder-30B-A3B-Instruct-GGUF:Q4_K_M

# Qualified 2026-07-14: 6-task agentic eval, 6/6 passed, tasks drawn from a
# local task manifest; the eval report run-eval.sh wrote under
# dev/local/audit-results/ is the evidence. Served via llama.cpp.
unsloth/Qwen3.6-27B-MTP-GGUF:UD-Q6_K_XL

# Qualified 2026-08-31: 6-task agentic eval, 6/6 passed, zero false claims
# (every transcript hand-read against the real gate). Tasks: agent-skills
# collect_purge_devlocal, gems executor.py fail-loud re-exec, doogat/ddb
# ConsistencyWarning::UnreadableFile, claude-warden env-danger matcher,
# claude-git-ferry branch-diff.bats, claude-aegis _common.py isatty guard -
# each isolated in a disposable git worktree reverted to its pre-task state
# (whole-file revert where safe, surgical function-level revert where the
# file had unrelated later history). Evidence: dev/local/audit-results/
# qwen-eval-unsloth-Qwen3.8-27B-GGUF-UD-Q6_K_XL-2026-08-31.md. Served via
# llama.cpp on :8002. Not yet promoted to default - see SKILL.md.
#
# Correction (still 2026-08-31): the ddb task's first pass was contaminated -
# a killed-and-restarted eval run left the file already fixed before the
# re-dispatch, so that transcript only proved the model could tidy a
# half-done git index, not implement the fix from scratch. Caught during
# post-hoc review, re-verified clean from a genuinely reverted pre-task
# state: cargo test -p ddb-core --lib, 1546 passed, 0 failed, exit 0. The
# other 5 tasks were independently confirmed to fail cleanly before their
# original dispatch, so only this one needed re-running.
unsloth/Qwen3.8-27B-GGUF:UD-Q6_K_XL
